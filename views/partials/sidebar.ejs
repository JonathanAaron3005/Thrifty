<style>
    .button {
        box-shadow: 0 5px 10px rgba(0, 0, 0, 0.3);
        border: none;
    }

    .button.zeround {
        border-radius: 15px;
        background-color: rgb(255, 255, 255);
    }

    .button.circle {
        border-radius: 100px;
    }

    .floaty {
        position: fixed; 
        width: 100px; 
        height: fit-content; 
        bottom: 10px; 
        right: 25px;
    }

    @media (max-width: 400px) {
        .large {
            display: none;
        }
    }
</style>

<div id="floaty" class="large floaty d-flex flex-column justify-content-center align-items-center" style="<%= currentUser === null ? 'display: none;' : ''%>">
    <button class="button zeround mb-3" style="width: 80%; height: 80px">
        profile
    </button>
    
    <button class="button zeround mb-3" style="width: 80%; height: 80px">
        cart
    </button>

    <button class="button circle orange" style="width: 100%; height: 100px">
        orders
    </button>
</div>

<script>
    function update(scroller) {
        var floatingDiv = document.getElementById('floaty');
        var footer = document.getElementById('foot');

        var y = scroller.scrollTop
        var y2 = (footer.offsetTop - 10) - window.innerHeight
        
        if (y > y2) {
            floatingDiv.style.bottom = (y - y2) + "px"
        } else {
            floatingDiv.style.bottom = ""
        }
    }

    window.addEventListener("DOMContentLoaded", function() {
        var scroller = document.getElementById('scroller');
        scroller.addEventListener("scroll", function() {
            update(scroller)
        })

        update(scroller);
    });
  </script>